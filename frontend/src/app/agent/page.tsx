import Placeholder from "@/components/Placeholder";
export default function Page() {
  return (
    <Placeholder
      title="Agent Console"
      blurb="Natural language → Qwen → Certainty Pipeline → SAF → Execute (live reasoning stream)."
    />
  );
}
