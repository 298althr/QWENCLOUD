# Docker — ALTHR Autopilot

Docker Compose files and Dockerfiles for the project.

## Files

- `docker-compose.yml` — Main compose file (backend + frontend + postgres + redis)
- `backend/Dockerfile` — Node.js backend image
- `frontend/Dockerfile` — Next.js frontend image

## Documentation

See `docs/deployment/docker-setup.md` for full configuration and setup instructions.
